# Ignore everything first
*

# Keep the folders you want to push
!dataset_interface/
!rft/

# Keep the .gitignore file itself
!.gitignore

# --- Add this line to ignore the share_models subfolder within rft ---
rft/share_models/